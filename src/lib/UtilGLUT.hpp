/*/////////////////////////////////////////////////////////////////////////////////////////////////
module:  declaration of GLUT utility class
purpose: implements general utilities for OpenGL/GLUT/OS support

written: U.Kuenzler
version: 2.00
/////////////////////////////////////////////////////////////////////////////////////////////////*/


// system includes ////////////////////////////////////////////////////////////////////////////////
#include <string>
using namespace std;

class UtilGLUT
///////////////////////////////////////////////////////////////////////////////////////////////////
{
public:
	static int getKeyboardModifier(int& state);
    static bool getFilePath(string& filepath,
        const string& filter = "All Files (*.*)\n*.*\n\0",
        const string& startdir = "" , bool open = true);

    static void showFPS(const string& title = "");
	static void showMessageBox(
        const string& msg,
        const string& title = "Error Message");
};
// class UtilGLUT /////////////////////////////////////////////////////////////////////////////////
